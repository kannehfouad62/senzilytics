import { prisma } from "@/lib/prisma";import type { ResearchDataRow,ResearchValue,ResearchVariable } from "@/modules/research/research-analysis";import { detectQualityIssues } from "@/modules/research/research-analysis";
export async function getResearchDataset(organizationId:string,collectionId:string){const collection=await prisma.researchCollectionWave.findFirst({where:{id:collectionId,organizationId},include:{project:{include:{client:true}},questionnaire:true,formVersion:{include:{fields:{orderBy:{sequence:"asc"}}}},datasetOwner:{select:{id:true,name:true}},datasetLockedBy:{select:{name:true}},datasetApprovedBy:{select:{name:true}},assignments:{where:{status:"COMPLETED",submissionId:{not:null}},include:{respondent:{select:{name:true,email:true}},reviewedBy:{select:{name:true}},submission:{include:{answers:true}}},orderBy:{completedAt:"asc"},take:5000}}});if(!collection)return null;const variables:ResearchVariable[]=collection.formVersion.fields.filter(field=>field.fieldType!=="FILE").map(field=>({id:field.id,key:field.key,label:field.label,type:field.fieldType,required:field.isRequired}));const assignmentRows=collection.assignments.map(assignment=>({assignment,row:(()=>{const byId=new Map(assignment.submission?.answers.map(answer=>[answer.fieldId,answer.value])??[]);return{assignmentId:assignment.id,responseId:assignment.submissionId!,submittedAt:assignment.completedAt!.toISOString(),values:Object.fromEntries(variables.map(variable=>[variable.key,(byId.get(variable.id)??null) as ResearchValue]))}})()}));const rows:ResearchDataRow[]=assignmentRows.map(item=>item.row);const analysisRows=assignmentRows.filter(item=>item.assignment.disposition==="INCLUDED").map(item=>item.row);return{collection,variables,rows,analysisRows,qualityIssues:detectQualityIssues(variables,rows)};}
export function listResearchDatasets(organizationId:string){return prisma.researchCollectionWave.findMany({where:{organizationId},include:{project:{include:{client:true}},questionnaire:true,_count:{select:{assignments:{where:{status:"COMPLETED"}}}},assignments:{where:{status:"COMPLETED",disposition:"FLAGGED"},select:{id:true}}},orderBy:{updatedAt:"desc"}})}
