import { createConfigurableForm } from "@/features/forms/actions";import { requirePermission } from "@/lib/permissions";import { ConfigurableFormModule,PermissionKey } from "@prisma/client";import Link from "next/link";
const input="mt-2 w-full rounded-xl border border-white/10 bg-slate-950/70 px-4 py-3";
export default async function NewFormPage({searchParams}:{searchParams:Promise<{error?:string}>}){await requirePermission(PermissionKey.MANAGE_ORGANIZATION);const{error}=await searchParams;return <div className="mx-auto max-w-3xl"><Link href="/form-studio" className="text-sm text-cyan-300">← Form Studio</Link><h1 className="mt-5 text-4xl font-bold">Create configurable form</h1><p className="mt-2 text-slate-400">Start with a version-one draft. It will not affect operations until deliberately published and integrated.</p>{error&&<p role="alert" className="mt-5 rounded-xl border border-red-400/20 bg-red-400/10 p-4 text-sm text-red-300">{error}</p>}<form action={createConfigurableForm} className="mt-8 space-y-5 rounded-3xl border border-white/10 bg-white/5 p-6"><label className="block">Form name<input name="name" required className={input}/></label><label className="block">Module<select name="module" required className={input}>{Object.values(ConfigurableFormModule).map(value=><option key={value} value={value}>{value.replaceAll("_"," ")}</option>)}</select></label><label className="block">Description<textarea name="description" rows={4} className={input}/></label><button className="rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-slate-950">Create Draft</button></form></div>}
