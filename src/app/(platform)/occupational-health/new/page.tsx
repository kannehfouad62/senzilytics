import { SurveillanceProgramCreateForm } from "@/features/occupational-health/surveillance-forms";import { requirePermission } from "@/lib/permissions";import { prisma } from "@/lib/prisma";import { getCurrentUserTenant } from "@/lib/tenant";import { PermissionKey } from "@prisma/client";import { ArrowLeft } from "lucide-react";import Link from "next/link";
export default async function NewSurveillanceProgramPage(){await requirePermission(PermissionKey.MANAGE_OCCUPATIONAL_HEALTH);const{organizationId}=await getCurrentUserTenant();const[users,agents,groups]=await Promise.all([prisma.user.findMany({where:{organizationId,isActive:true},select:{id:true,name:true},orderBy:{name:"asc"}}),prisma.hygieneAgent.findMany({where:{organizationId,isActive:true},select:{id:true,name:true},orderBy:{name:"asc"}}),prisma.similarExposureGroup.findMany({where:{organizationId,isActive:true},select:{id:true,name:true},orderBy:{name:"asc"}})]);return <div className="mx-auto max-w-5xl"><Link href="/occupational-health" className="inline-flex items-center gap-2 text-sm text-slate-400"><ArrowLeft size={16}/>Back to Occupational Health</Link><h1 className="mt-5 text-4xl font-bold">Create Surveillance Program</h1><p className="mt-2 text-slate-400">Configure administrative coverage, recurrence, ownership, and exposure linkage.</p><SurveillanceProgramCreateForm users={users} agents={agents} groups={groups}/></div>}
