import { getCurrentUserPermissions,requirePermission } from "@/lib/permissions";import { prisma } from "@/lib/prisma";import { getCurrentUserTenant } from "@/lib/tenant";import { PermissionKey,SurveillanceEnrollmentStatus,SurveillanceProgramStatus } from "@prisma/client";import { HeartPulse,Plus } from "lucide-react";import Link from "next/link";
export default async function OccupationalHealthPage(){await requirePermission(PermissionKey.VIEW_OCCUPATIONAL_HEALTH);const[{organizationId},permissions]=await Promise.all([getCurrentUserTenant(),getCurrentUserPermissions()]);const programs=await prisma.medicalSurveillanceProgram.findMany({where:{organizationId},include:{agent:true,group:true,responsibleUser:true,enrollments:true},orderBy:{name:"asc"}});const canManage=permissions.includes(PermissionKey.MANAGE_OCCUPATIONAL_HEALTH),active=programs.filter(x=>x.status===SurveillanceProgramStatus.ACTIVE).length,enrollments=programs.flatMap(x=>x.enrollments),due=enrollments.filter(x=>x.status===SurveillanceEnrollmentStatus.DUE||x.status===SurveillanceEnrollmentStatus.OVERDUE).length,restricted=enrollments.filter(x=>x.fitnessOutcome==="CLEARED_WITH_RESTRICTIONS"||x.fitnessOutcome==="TEMPORARILY_NOT_CLEARED").length;return <div><div className="flex flex-wrap justify-between gap-4"><div><p className="flex items-center gap-2 text-sm text-cyan-300"><HeartPulse size={17}/>Privacy-Conscious Workforce Assurance</p><h1 className="mt-2 text-4xl font-bold">Occupational Health</h1><p className="mt-2 max-w-3xl text-slate-400">Administer exposure-based medical surveillance, recurring due dates, provider certificates, and fitness-for-work outcomes without storing diagnoses or clinical test details.</p></div>{canManage&&<Link href="/occupational-health/new" className="inline-flex h-fit items-center gap-2 rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-slate-950"><Plus size={17}/>New Program</Link>}</div><div className="mt-7 grid gap-4 sm:grid-cols-4"><Metric label="Programs" value={programs.length}/><Metric label="Active" value={active}/><Metric label="Due / overdue" value={due} danger={due>0}/><Metric label="Work restrictions" value={restricted} danger={restricted>0}/></div><p className="mt-7 rounded-xl border border-amber-400/20 bg-amber-400/5 p-4 text-sm text-amber-200">Restricted workspace: record only administrative surveillance status and provider-issued fitness-for-work outcomes. Do not enter diagnoses, treatments, laboratory results, or clinical notes.</p><div className="mt-6 grid gap-4">{programs.map(x=><Link key={x.id} href={`/occupational-health/${x.id}`} className="rounded-3xl border border-white/10 bg-white/5 p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="font-semibold">{x.name}</p><p className="mt-2 text-sm text-slate-400">{x.agent?.name||"General surveillance"} · {x.group?.name||"No specific SEG"} · Every {x.frequencyMonths} month(s)</p></div><span className="text-xs text-cyan-300">{x.status}</span></div><p className="mt-4 text-xs text-slate-500">{x.enrollments.length} enrolled · Owner {x.responsibleUser.name}</p></Link>)}</div>{!programs.length&&<p className="mt-8 rounded-3xl border border-dashed border-white/10 p-10 text-center text-slate-500">No occupational health surveillance programs have been created.</p>}</div>}
function Metric({label,value,danger=false}:{label:string;value:number;danger?:boolean}){return <div className="rounded-2xl border border-white/10 bg-white/5 p-5"><p className="text-xs uppercase text-slate-500">{label}</p><p className={`mt-2 text-3xl font-bold ${danger?"text-red-300":"text-white"}`}>{value}</p></div>}
