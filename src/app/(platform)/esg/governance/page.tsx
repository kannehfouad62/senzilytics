import { createEsgInitiative,createEsgTarget } from "@/features/esg/operational.actions";import { requirePermission } from "@/lib/permissions";import { prisma } from "@/lib/prisma";import { getCurrentUserTenant } from "@/lib/tenant";import { EsgPillar,PermissionKey } from "@prisma/client";const input="mt-2 w-full rounded-xl border border-white/10 bg-slate-950/70 px-4 py-3";
export default async function Governance(){await requirePermission(PermissionKey.MANAGE_ESG);const{organizationId}=await getCurrentUserTenant();const[metrics,users,targets,initiatives]=await Promise.all([prisma.esgMetricDefinition.findMany({where:{organizationId,isActive:true}}),prisma.user.findMany({where:{organizationId}}),prisma.esgTarget.findMany({where:{metric:{organizationId}},include:{metric:true}}),prisma.esgInitiative.findMany({where:{organizationId},include:{owner:true}})]);return <div><h1 className="text-4xl font-bold">ESG Targets & Initiatives</h1><div className="mt-8 grid gap-6 lg:grid-cols-2"><form action={createEsgTarget} className="space-y-3 rounded-3xl border border-white/10 bg-white/5 p-6"><h2 className="text-xl font-semibold">Create Target</h2><select name="metricId" className={input}>{metrics.map(x=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select><div className="grid grid-cols-2 gap-3"><input type="number" name="baselineYear" placeholder="Baseline year" required className={input}/><input type="number" step="any" name="baselineValue" placeholder="Baseline value" required className={input}/><input type="number" name="targetYear" placeholder="Target year" required className={input}/><input type="number" step="any" name="targetValue" placeholder="Target value" required className={input}/></div><textarea name="description" className={input}/><button className="rounded-xl bg-cyan-300 px-4 py-2 text-slate-950">Create Target</button></form><form action={createEsgInitiative} className="space-y-3 rounded-3xl border border-white/10 bg-white/5 p-6"><h2 className="text-xl font-semibold">Create Initiative</h2><input name="name" placeholder="Initiative name" required className={input}/><select name="pillar" className={input}>{Object.values(EsgPillar).map(x=><option key={x}>{x}</option>)}</select><select name="ownerId" className={input}><option value="">Unassigned</option>{users.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><div className="grid grid-cols-2 gap-3"><input type="date" name="startDate" className={input}/><input type="date" name="targetDate" className={input}/></div><input type="number" step="any" name="budget" placeholder="Budget" className={input}/><textarea name="expectedOutcome" placeholder="Expected outcome" className={input}/><textarea name="description" placeholder="Description" className={input}/><button className="rounded-xl bg-cyan-300 px-4 py-2 text-slate-950">Create Initiative</button></form></div><div className="mt-8 grid gap-4 lg:grid-cols-2"><section>{targets.map(x=><div key={x.id} className="mb-3 rounded-2xl border border-white/10 bg-white/5 p-5"><p className="font-semibold">{x.metric.name}</p><p className="text-sm text-slate-400">{x.baselineValue} ({x.baselineYear}) → {x.targetValue} ({x.targetYear})</p></div>)}</section><section>{initiatives.map(x=><div key={x.id} className="mb-3 rounded-2xl border border-white/10 bg-white/5 p-5"><p className="font-semibold">{x.name}</p><p className="text-sm text-slate-400">{x.pillar} · {x.status} · {x.owner?.name||"Unassigned"}</p></div>)}</section></div></div>}
