import { importEnvironmentalCsv } from "@/features/environmental/import.actions";import { requirePermission } from "@/lib/permissions";import { PermissionKey } from "@prisma/client";
export default async function Import(){await requirePermission(PermissionKey.MANAGE_ENVIRONMENTAL);return <div className="mx-auto max-w-4xl"><h1 className="text-4xl font-bold">Import Environmental Data</h1><p className="mt-2 text-slate-400">Use unquoted comma-separated values. Existing reporting-period records are rejected to protect approved history.</p><form action={importEnvironmentalCsv} className="mt-8 rounded-3xl border border-white/10 bg-white/5 p-6"><textarea name="csv" required rows={16} defaultValue="metricCode,siteId,periodStart,periodEnd,value,quality,evidenceSummary" className="w-full rounded-xl bg-slate-950/70 p-4 font-mono text-sm"/><button className="mt-4 rounded-xl bg-cyan-300 px-5 py-3 text-slate-950">Import CSV</button></form></div>}
