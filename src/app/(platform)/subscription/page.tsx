import { getCurrentUserTenant } from "@/lib/tenant";
import { getOrganizationSubscription } from "@/lib/subscription";
import { Check, LockKeyhole } from "lucide-react";

export default async function SubscriptionPage(){const{organizationId}=await getCurrentUserTenant();const subscription=await getOrganizationSubscription(organizationId);return <div className="mx-auto max-w-4xl"><p className="text-sm text-cyan-300">Organization Subscription</p><h1 className="mt-2 text-4xl font-bold">{pretty(subscription.subscriptionPlan)} Plan</h1><p className="mt-3 text-slate-400">Some capabilities are controlled by your organization’s Senzilytics agreement.</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{Object.entries(subscription.entitlements).map(([feature,enabled])=><div key={feature} className="flex items-center gap-3 rounded-2xl border border-white/10 bg-white/5 p-5">{enabled?<Check className="text-emerald-300"/>:<LockKeyhole className="text-slate-600"/>}<div><p className="font-medium">{pretty(feature)}</p><p className="mt-1 text-xs text-slate-500">{enabled?"Included":"Upgrade required"}</p></div></div>)}</div><div className="mt-8 rounded-3xl border border-cyan-400/20 bg-cyan-400/[.05] p-6"><h2 className="text-xl font-semibold">Need another capability?</h2><p className="mt-2 text-slate-400">Ask your organization administrator or contact Senzilytics to discuss a plan upgrade.</p><a href="mailto:info@senzilytics.com?subject=Senzilytics%20Plan%20Upgrade" className="mt-5 inline-block rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-slate-950">Contact Senzilytics</a></div></div>}
function pretty(value:string){return value.replaceAll("_"," ").toLowerCase().replace(/\b\w/g,letter=>letter.toUpperCase())}
