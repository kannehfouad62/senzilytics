import { isAuthorizedCronRequest } from "@/lib/cron-auth";import { processEnvironmentalMonitoring } from "@/modules/environmental/environmental-monitor.service";import { NextRequest,NextResponse } from "next/server";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(r:NextRequest){if(!isAuthorizedCronRequest(r.headers.get("authorization")))return NextResponse.json({success:false,error:"Unauthorized."},{status:401});return NextResponse.json({success:true,processedAt:new Date().toISOString(),environmental:await processEnvironmentalMonitoring()})}
