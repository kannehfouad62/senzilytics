import { isAuthorizedCronRequest } from "@/lib/cron-auth";import { processChemicalMonitoring } from "@/modules/chemical/chemical-monitor.service";import { NextRequest,NextResponse } from "next/server";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(r:NextRequest){if(!isAuthorizedCronRequest(r.headers.get("authorization")))return NextResponse.json({success:false,error:"Unauthorized."},{status:401});try{return NextResponse.json({success:true,processedAt:new Date().toISOString(),chemicals:await processChemicalMonitoring()})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Chemical monitoring failed."},{status:500})}}
