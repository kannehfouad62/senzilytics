import{PermissionKey}from"@prisma/client";import{requirePermission}from"@/lib/permissions";import{getCurrentUserTenant}from"@/lib/tenant";import{prisma}from"@/lib/prisma";import{createResearchPresentation,type SlideElement}from"@/modules/research/research-presentation";export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(_request:Request,{params}:{params:Promise<{analysisId:string}>}){await requirePermission(PermissionKey.EXPORT_RESEARCH_OUTPUTS);const[{analysisId},{organizationId}]=await Promise.all([params,getCurrentUserTenant()]),analysis=await prisma.researchAnalysis.findFirst({where:{id:analysisId,organizationId},include:{collection:{include:{project:{include:{client:true}}}},analyst:{select:{name:true}},approvedBy:{select:{name:true}}}});if(!analysis)return new Response("Analysis not found.",{status:404});const title:SlideElement[]=[{x:.7,y:.7,w:10,h:.4,text:"SENZILYTICS GOVERNED MODEL",size:13,color:"67E8F9",bold:true},{x:.7,y:1.5,w:11.5,h:.8,text:analysis.title,size:30,bold:true},{x:.7,y:2.6,w:11,h:.4,text:`${analysis.collection.project.reference} · ${analysis.method.replaceAll("_"," ")} · v${analysis.version}`,size:16,color:"94A3B8"},{x:.7,y:3.6,w:10,h:.4,text:`Analytical population: ${analysis.datasetResponseCount}`,size:16,color:"CBD5E1"},{x:.7,y:4.2,w:10,h:.4,text:`Governance status: ${analysis.status}`,size:16,color:"CBD5E1"}];const results:SlideElement[]=[{x:.7,y:.5,w:11,h:.5,text:"Model Results & Diagnostics",size:26,bold:true},...lines(analysis.resultSnapshot).slice(0,14).map((text,index)=>({x:.8+(index%2)*6,y:1.4+Math.floor(index/2)*.68,w:5.5,h:.38,text,size:12,color:"CBD5E1"}))];const governance:SlideElement[]=[{x:.7,y:.5,w:11,h:.5,text:"Governance & Interpretation",size:26,bold:true},{x:.8,y:1.5,w:11,h:.5,text:`Analyst: ${analysis.analyst.name??"Assigned analyst"}`,size:16,color:"CBD5E1"},{x:.8,y:2.2,w:11,h:.5,text:`Approved by: ${analysis.approvedBy?.name??"Pending independent approval"}`,size:16,color:"CBD5E1"},{x:.8,y:3,w:11,h:1,text:`Hypothesis: ${analysis.hypothesis??"Not recorded"}`,size:14,color:"CBD5E1"},{x:.8,y:4.2,w:11,h:1.2,text:`Methodology: ${analysis.methodologyNotes??"Not recorded"}`,size:14,color:"CBD5E1"},{x:.8,y:6.2,w:11,h:.4,text:"Model outputs require qualified review of assumptions, diagnostics, bias and practical significance.",size:10,color:"64748B"}];const output=await createResearchPresentation([title,results,governance]),body=new Uint8Array(output.byteLength);body.set(output);return new Response(body,{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.presentationml.presentation","Content-Disposition":`attachment; filename="${analysis.collection.project.reference}-model-v${analysis.version}.pptx"`,"Cache-Control":"private, no-store"}})}function lines(value:unknown,path="result"):string[]{if(value===null||value===undefined)return[];if(typeof value!=="object")return[`${path.replaceAll("."," › ")}: ${typeof value==="number"?Number(value.toFixed(4)):value}`];return(Array.isArray(value)?value.map((item,index)=>[String(index),item]as const):Object.entries(value)).flatMap(([key,item])=>lines(item,`${path}.${key}`))}
