import { requestPasswordReset } from "@/features/identity/password-reset.actions";
import Link from "next/link";
export default async function ForgotPassword({searchParams}:{searchParams:Promise<{sent?:string}>}){const{sent}=await searchParams;return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-6 text-white"><form action={requestPasswordReset} className="w-full max-w-md rounded-3xl border border-white/10 bg-white/5 p-8"><p className="text-sm text-cyan-300">Account Recovery</p><h1 className="mt-2 text-3xl font-bold">Forgot password</h1><p className="mt-3 text-sm text-slate-400">Enter your account email. If it is eligible, we will send a secure one-hour reset link.</p>{sent&&<p className="mt-5 rounded-xl bg-emerald-400/10 p-4 text-sm text-emerald-300">If an eligible account exists, the reset email has been sent.</p>}<input name="email" type="email" required placeholder="you@company.com" className="mt-6 w-full rounded-xl border border-white/10 bg-slate-950 px-4 py-3"/><button className="mt-4 w-full rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-slate-950">Send Reset Link</button><Link href="/login" className="mt-5 block text-center text-sm text-cyan-300">Back to sign in</Link></form></main>}
