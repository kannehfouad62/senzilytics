"use client";
import { useState } from "react";
type Field={id:string;key:string;label:string;description:string|null;placeholder:string|null;fieldType:string;isRequired:boolean;options:unknown;visibilityRule:unknown};
type Form={id:string;name:string;description:string|null;version:{id:string;version:number;instructions:string|null;fields:Field[]}};
const input="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/70 px-4 py-3 text-white";
const ruleOf=(value:unknown)=>value&&typeof value==="object"&&!Array.isArray(value)?value as {fieldKey?:string;operator?:string;value?:string}:null;
export function RuntimeFormFields({forms}:{forms:Form[]}){const[values,setValues]=useState<Record<string,string|string[]|boolean>>({});if(!forms.length)return null;const visible=(field:Field)=>{const rule=ruleOf(field.visibilityRule);if(!rule?.fieldKey||rule.operator!=="EQUALS")return true;const actual=values[rule.fieldKey];return Array.isArray(actual)?actual.includes(rule.value||""):String(actual??"")===String(rule.value??"")};return <div className="space-y-6">{forms.map(form=><section key={form.id} className="rounded-3xl border border-cyan-400/20 bg-cyan-400/5 p-6"><p className="text-xs font-semibold uppercase tracking-wide text-cyan-300">Organization-specific form · v{form.version.version}</p><h2 className="mt-2 text-xl font-semibold">{form.name}</h2>{form.description&&<p className="mt-1 text-sm text-slate-400">{form.description}</p>}{form.version.instructions&&<p className="mt-3 rounded-xl bg-slate-950/40 p-3 text-sm text-slate-300">{form.version.instructions}</p>}<div className="mt-5 grid gap-5 md:grid-cols-2">{form.version.fields.filter(visible).map(field=><RuntimeField key={field.id} field={field} onChange={value=>setValues(current=>({...current,[field.key]:value}))}/>)}</div></section>)}</div>}
function RuntimeField({field,onChange}:{field:Field;onChange:(value:string|string[]|boolean)=>void}){const name=`custom_${field.id}`,options=Array.isArray(field.options)?field.options.filter((x):x is string=>typeof x==="string"):[],label=<span className="text-sm text-slate-300">{field.label}{field.isRequired&&<span className="text-red-300"> *</span>}{field.description&&<span className="mt-1 block text-xs text-slate-500">{field.description}</span>}</span>;if(field.fieldType==="FILE")return <div>{label}<p className="mt-2 rounded-xl border border-amber-400/20 bg-amber-400/5 p-3 text-xs text-amber-200">This private attachment will be uploaded immediately after the observation record is created.</p></div>;if(field.fieldType==="BOOLEAN")return <label className="flex items-start gap-3">{<input type="checkbox" name={name} required={field.isRequired} onChange={event=>onChange(event.target.checked)} className="mt-1"/>}<span>{label}</span></label>;if(field.fieldType==="LONG_TEXT")return <label>{label}<textarea name={name} required={field.isRequired} rows={4} placeholder={field.placeholder||""} onChange={event=>onChange(event.target.value)} className={input}/></label>;if(field.fieldType==="SINGLE_SELECT")return <label>{label}<select name={name} required={field.isRequired} defaultValue="" onChange={event=>onChange(event.target.value)} className={input}><option value="">Select</option>{options.map(option=><option key={option}>{option}</option>)}</select></label>;if(field.fieldType==="MULTI_SELECT")return <label>{label}<select name={name} required={field.isRequired} multiple onChange={event=>onChange([...event.target.selectedOptions].map(option=>option.value))} className={input}>{options.map(option=><option key={option}>{option}</option>)}</select></label>;return <label>{label}<input name={name} required={field.isRequired} type={field.fieldType==="NUMBER"?"number":field.fieldType==="DATE"?"date":field.fieldType==="DATETIME"?"datetime-local":field.fieldType==="EMAIL"?"email":field.fieldType==="PHONE"?"tel":"text"} placeholder={field.placeholder||""} onChange={event=>onChange(event.target.value)} className={input}/></label>}
